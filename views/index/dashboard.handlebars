<h1>Welcome {{user.firstName}}</h1>
<h4>Your Reports</h4>
{{#if reports}}
<table class="striped">
  <thead>
    <tr>
      <th>Title</th>
      <th>Date</th>
      <th>Status</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    {{#each reports}}
    <tr>
      <td><a href="/reports/show/{{id}}">{{title}}</a></td>
      <td>{{formatDate data 'MMMM do YYYY'}}</td>
      <td><span class="dash-status">{{status}}</span></td>
      <td>
        <a class="btn left-align" href="/reports/edit/{{id}}"><i class="fa fa-pencil"></i> Edit</a>

        <form action="/reports/{{id}}?_method=DELETE" method="post" id="delete-form">
          <input type="hidden" name="_method" value="DELETE">
          <button type="submit" class="btn red"><i class="fa fa-remove"></i> Delete</button>
        </form>
      </td>
    </tr>
    {{/each}}
  </tbody>
</table>
{{else}}
<p>You have not created any reports yet</p>
{{/if}}